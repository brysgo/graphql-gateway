{
  "name": "graphql-gateway",
  "version": "0.1.0",
  "description": "Put a bunch of graphql services behind one schema",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "coverage": "jest --coverage",
    "coverage:watch": "jest --coverage --watch"
  },
  "repository": "git@github.com:brysgo/graphql-gateway.git",
  "author": "Bryan Goldstein",
  "license": "MIT",
  "dependencies": {
    "array.prototype.flat": "^1.2.1",
    "graphql": "^14.0.0",
    "graphql-mask": "^1.0.12",
    "merge-resolver": "^0.1.1"
  },
  "devDependencies": {
    "jest-cli": "^23.0.0"
  }
}
